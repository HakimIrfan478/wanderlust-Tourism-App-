# Lightweight dependency set: everything except torch / sentence-transformers.
# Used by CI, and by anyone who cannot install the ML stack. The application
# runs unchanged; the semantic recommender simply reports itself unavailable
# and the TF-IDF baseline serves requests.
#
# Pair this with DISABLE_SEMANTIC_MODEL=1.

Django==5.0.6
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
requests==2.31.0

scikit-learn>=1.4
numpy>=1.26
