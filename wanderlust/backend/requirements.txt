# Full development / demo environment.
# Install with:  pip install -r requirements.txt
#
# For a machine that cannot install torch, use requirements-ci.txt instead:
# the app runs identically minus the semantic recommender, which reports
# itself as unavailable rather than failing.

# --- Core backend ---
Django==5.0.6
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
requests==2.31.0            # Open-Meteo, REST Countries, Wikipedia images

# --- Database ---
# Only needed when USE_SQLITE=0. Harmless otherwise.
psycopg2-binary==2.9.9

# --- Recommender: classical baseline (the control condition) ---
scikit-learn>=1.4
numpy>=1.26

# --- Recommender: semantic condition ---
# Pulls in torch, which is large. If it will not install on your platform,
# set DISABLE_SEMANTIC_MODEL=1 and the TF-IDF baseline carries the app.
sentence-transformers==2.7.0
